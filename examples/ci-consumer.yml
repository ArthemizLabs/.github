name: CI (Consumidor Exemplo)

on:
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  ci:
    uses: ArthemizLabs/.github/.github/workflows/ci.yml@<tag-ou-sha>
    with:
      workdir: "."
      node-version: "20"
      python-version: "3.12"
      go-version: "1.22"
