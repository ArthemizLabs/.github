name: Deploy to GitHub Pages (Wrapper)

on:
  push:
    branches:
      - main

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy:
    uses: ./.github/workflows/pages.yml
    with:
      artifact_path: "./site"
      build_command: ""
      build_workdir: "."
