name: CI (Wrapper)

on:
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  ci:
    uses: ./.github/workflows/ci.yml
    with:
      workdir: "."
      node-version: "20"
      python-version: "3.12"
      go-version: "1.22"
