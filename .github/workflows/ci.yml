name: CI
permissions:
  contents: read

on:
  workflow_call:
    inputs:
      workdir:
        description: "Diretório onde os comandos serão executados."
        required: false
        default: "."
        type: string
      node-version:
        description: "Versão do Node.js a ser utilizada."
        required: false
        default: "20"
        type: string
      python-version:
        description: "Versão do Python a ser utilizada."
        required: false
        default: "3.12"
        type: string
      go-version:
        description: "Versão do Go a ser utilizada (usada junto ao go.mod)."
        required: false
        default: "1.22"
        type: string
    secrets:
      github_token:
        description: "Token para operações do checkout. Usa o GITHUB_TOKEN do repositório por padrão."
        required: false

jobs:
  build:
    name: Lint, Test, and Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.github_token || secrets.GITHUB_TOKEN }}

      - name: Validar diretório de trabalho
        working-directory: .
        run: |
          if [ ! -d "${{ inputs.workdir }}" ]; then
            echo "Diretório de trabalho não encontrado: ${{ inputs.workdir }}"
            exit 1
          fi

      - name: Detect runtime
        id: detect
        working-directory: ${{ inputs.workdir }}
        run: |
          if [ -f "package.json" ]; then
            echo "runtime=node" >> "$GITHUB_OUTPUT"
          elif [ -f "go.mod" ]; then
            echo "runtime=go" >> "$GITHUB_OUTPUT"
          elif [ -f "requirements.txt" ] || [ -f "pyproject.toml" ]; then
            echo "runtime=python" >> "$GITHUB_OUTPUT"
          else
            echo "runtime=none" >> "$GITHUB_OUTPUT"
          fi

      # Node.js setup
      - name: Set up Node.js
        if: steps.detect.outputs.runtime == 'node'
        uses: actions/setup-node@v4
        with:
          node-version: ${{ inputs['node-version'] }}
          cache: "npm"
          cache-dependency-path: ${{ inputs.workdir }}/package-lock.json

      - name: Install Node.js dependencies
        if: steps.detect.outputs.runtime == 'node'
        working-directory: ${{ inputs.workdir }}
        run: |
          if [ -f package-lock.json ]; then
            npm ci
          else
            echo "Erro: package-lock.json não encontrado. Crie e versione um lockfile para builds determinísticos no CI."
            exit 1
          fi

      - name: Lint (Node.js)
        if: steps.detect.outputs.runtime == 'node'
        working-directory: ${{ inputs.workdir }}
        run: |
          node -e "const fs=require('fs');const pkg=JSON.parse(fs.readFileSync('package.json'));if(!pkg.scripts||!pkg.scripts.lint){console.error('Erro: script \"lint\" não definido em package.json. Configure scripts.lint para habilitar lint no CI.');process.exit(1);}"
          npm run lint

      - name: Run tests (Node.js)
        if: steps.detect.outputs.runtime == 'node'
        working-directory: ${{ inputs.workdir }}
        run: |
          node -e "const fs=require('fs');const pkg=JSON.parse(fs.readFileSync('package.json'));if(!pkg.scripts||!pkg.scripts.test){console.error('Erro: script \"test\" não definido em package.json. Configure scripts.test para habilitar testes no CI.');process.exit(1);}"
          npm test

      # Go setup
      - name: Set up Go
        if: steps.detect.outputs.runtime == 'go'
        uses: actions/setup-go@v5
        with:
          go-version: ${{ inputs['go-version'] }}
          cache: true

      - name: Install Go dependencies
        if: steps.detect.outputs.runtime == 'go'
        working-directory: ${{ inputs.workdir }}
        run: go mod download

      - name: Lint (Go)
        if: steps.detect.outputs.runtime == 'go'
        uses: golangci/golangci-lint-action@v6
        with:
          version: latest
          working-directory: ${{ inputs.workdir }}

      - name: Run tests (Go)
        if: steps.detect.outputs.runtime == 'go'
        working-directory: ${{ inputs.workdir }}
        run: go test ./... -race -coverprofile=coverage.out

      # Python setup
      - name: Set up Python
        if: steps.detect.outputs.runtime == 'python'
        uses: actions/setup-python@v5
        with:
          python-version: ${{ inputs['python-version'] }}
          cache: "pip"

      - name: Install Python dependencies
        if: steps.detect.outputs.runtime == 'python'
        working-directory: ${{ inputs.workdir }}
        run: |
          pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
          if [ -f requirements-dev.txt ]; then pip install -r requirements-dev.txt; fi
          if [ -f pyproject.toml ]; then pip install -e ".[dev]"; fi

      - name: Lint (Python)
        if: steps.detect.outputs.runtime == 'python'
        working-directory: ${{ inputs.workdir }}
        run: |
          pip install ruff
          ruff check .

      - name: Run tests (Python)
        if: steps.detect.outputs.runtime == 'python'
        working-directory: ${{ inputs.workdir }}
        run: |
          pip install pytest
          pytest --tb=short

      - name: No recognized runtime detected
        if: steps.detect.outputs.runtime == 'none'
        run: |
          echo "No recognized runtime detected (Node.js, Go, or Python). Skipping dependency installation, lint, and test steps."
          echo "Add support for your runtime to .github/workflows/ci.yml."
